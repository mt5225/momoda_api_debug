
/**
 * 创建物体
 * @param {string} 模型
 * @param {BaseObject} [c] 父物体
 * @param {function} [c] 回调
 * @param {Vector3} [c] 位置
 * @param {Quad} [c] 旋转
 * @param {Vector3} [c] 缩放
 * @returns {BaseObject} 物体
 */
创建物体 = function(模型, 父物体, 回调, 位置, 旋转, 缩放) {
	var obj = new BaseObject();
    return obj;
};
